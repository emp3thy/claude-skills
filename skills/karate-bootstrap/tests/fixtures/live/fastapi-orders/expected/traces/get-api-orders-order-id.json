{
  "id": "GET /api/orders/{order_id}",
  "auth": "required",
  "responses": [
    { "status": 200, "when": "order found" },
    { "status": 404, "when": "no order with that id" }
  ],
  "reads": [
    { "kind": "db-read", "table": "orders",
      "via": "{{line:app/main.py:session.get}}" }
  ],
  "exits": [],
  "exits_none_reason": "read-only lookup",
  "unresolved": []
}
