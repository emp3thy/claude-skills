[
  {
    "fingerprint": "355885f54c1b8168",
    "verdict": "confirm",
    "proof": "Grep across the repo for `newCheckout` and `isEnabled(` finds only the literal FLAGS entry (flags.ts:3) and the single literal call site in checkout.ts:7 \u2014 no env var, config loader, admin toggle, or test overrides FLAGS. client.ts and client-admin.ts use isEnabled only with their own distinct literal flag names (betaBanner, adminPanel), never newCheckout. index.ts calls checkout() directly, not via a router/DI/reflection path, so the `new:` branch (checkout.ts:7-9) is genuinely unreachable in any current code path.",
    "severity": 2,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "reflection",
      "string-dispatch",
      "env/config override",
      "entry point"
    ],
    "opened": [
      "src/api/client.ts",
      "src/api/client-admin.ts",
      "src/index.ts"
    ]
  },
  {
    "fingerprint": "9c6b39118019c27e",
    "verdict": "downgrade",
    "severity": 2,
    "proof": "Same underlying fact as the dead-code candidate: no toggle mechanism exists anywhere (verified via grep of isEnabled/FLAGS and referrers client.ts, client-admin.ts, index.ts), so call-site ratio is indeed 100% legacy. However this is a single 3-line unreachable if-block with a churn=2/fan_in=3/hotspot=0 footprint and no external dependents on the 'new:' format \u2014 removal is a one-line-diff deletion, not migration-scale work, so severity 3 overstates impact; 2 fits better alongside the near-identical candidate 1.",
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "string-dispatch",
      "env/config override",
      "call-site ratio"
    ],
    "opened": [
      "src/api/client.ts",
      "src/api/client-admin.ts",
      "src/index.ts"
    ]
  },
  {
    "fingerprint": "f814e83b9c9e56a4",
    "verdict": "confirm",
    "proof": "Confirmed: no ticket reference found anywhere in flags.ts or checkout.ts, and no toggle exists (same grep evidence as candidates 1/2). The isEnabled(\"newCheckout\") stub is a concrete conditional branch, not an abstract contract, so it is genuinely a half-finished/abandoned path rather than intentional API surface. Effort is overstated as M in the note \u2014 deleting checkout.ts:7-9 and the now-unused `total` import is a trivial single-file change, so S is more accurate.",
    "severity": 2,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "ticket reference",
      "abstract contract check"
    ],
    "opened": [
      "src/api/client.ts",
      "src/api/client-admin.ts",
      "src/index.ts"
    ]
  },
  {
    "fingerprint": "795ec415100325e3",
    "verdict": "confirm",
    "proof": "checkout.ts:11 calls legacyFormat(priceOf(...)) unconditionally on the default path (since newCheckout is always false per candidates 1-3), making this deprecated function the actual production formatter, not dead legacy code. No ticket found referencing removal. format.ts:1-3 shows formatMoney is the only intended replacement and legacyFormat is its sole caller-facing wrapper (format-legacy.ts:5), confirming the deprecated code is load-bearing.",
    "severity": 2,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "ticket reference",
      "call-site liveness"
    ],
    "opened": [
      "src/util/format.ts"
    ]
  },
  {
    "fingerprint": "b7396d04c3285472",
    "verdict": "confirm",
    "proof": "Grep confirms legacyFormat's only caller is checkout.ts:11, and formatMoney (format.ts:1) has no direct callers outside format-legacy.ts:5, matching the '1 of 1 call site on old side' claim. Note: legacyFormat isn't a pure pass-through \u2014 it rewrites '$' to 'USD ' (format-legacy.ts:5), so migrating callers to formatMoney directly would change output format, meaning the deprecation is real debt but not a no-op removal. No removal date exists in either file.",
    "severity": 2,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "call-site ratio",
      "behavioral equivalence of old vs new"
    ],
    "opened": [
      "src/util/format.ts"
    ]
  },
  {
    "fingerprint": "3d5daf8fb72b00a9",
    "verdict": "confirm",
    "proof": "package.json:6 lists only typescript, jest, and eslint as devDependencies \u2014 tslint is absent \u2014 and scripts (package.json:5) only define build/test, with no lint script for either config. ci.yml confirms the only CI step is `npm test` (jest); no lint step runs eslint or tslint. tslint.json is therefore a genuinely inert leftover with zero tooling behind it, while .eslintrc.json at least has its dependency present even though unreferenced by scripts.",
    "severity": 2,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "devDependency presence",
      "script/CI references"
    ],
    "opened": [
      ".github/workflows/ci.yml"
    ]
  }
]
