[
  {
    "fingerprint": "2e969b3499636196",
    "verdict": "confirm",
    "proof": "refund.py:31-34 catches bare Exception from ledger.post and passes silently — no log, no re-raise, no cause preserved. Execution then proceeds unconditionally to gateway.refund at line 37, which moves money via the live v2 API (gateway.py) regardless of ledger write success. This means a ledger write failure produces no audit record and no operator visibility while the refund is still issued, a genuine financial-integrity error-masking bug.",
    "severity": 5,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "control flow from ledger.post through gateway.refund",
      "whether failure is logged or surfaced elsewhere",
      "gateway.py to confirm it performs a real state-changing call"
    ],
    "opened": [
      "src/pay/gateway.py",
      "src/pay/models.py"
    ]
  },
  {
    "fingerprint": "4b10ada7bef4b500",
    "verdict": "confirm",
    "proof": "Grep confirms no test calls issue_partial anywhere in tests/. Unlike test_issue_calls_gateway (explicitly marked @pytest.mark.skip), issue_partial has no test stub at all — not even a skipped placeholder. The fraction-to-cents conversion (refund.py:46) and full delegation to issue() are unguarded, so a rounding or scale regression would ship undetected.",
    "severity": 3,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "grep for 'issue_partial' in tests/",
      "presence of skip markers as a signal of intentional deferral"
    ],
    "opened": []
  },
  {
    "fingerprint": "74967b275a61fb33",
    "verdict": "confirm",
    "proof": "refund.py:39-40 uses `log.exception` for the OSError path, but line 41 uses bare `print()` for the normal-outcome message in the same function, on the same code path a few lines later. There is no __main__ guard or CLI parsing in this module (it's a library import per refund.py:1), so the print goes to stdout uncontrolled by log level/handlers, breaking aggregation for a production refund outcome. Real, low-effort fix (swap to log.info).",
    "severity": 3,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "module has no __main__ guard",
      "log is used elsewhere in same function",
      "no CLI parsing present"
    ],
    "opened": [
      "src/pay/gateway.py",
      "src/pay/ledger.py",
      "tests/test_refund.py"
    ]
  },
  {
    "fingerprint": "b459099b48e0a272",
    "verdict": "confirm",
    "proof": "FIXME at refund.py:35 names duplicate-refund risk from gateway-side retries. The only dedup guard is `_seen` (refund.py:14,27-29), an in-memory, per-process set keyed on order_id at the start of issue() -- it does not gate the actual gateway.refund() call from being retried by the gateway itself, and resets on process restart. No ticket reference found in code or tests. The named risk is genuinely still present: nothing in gateway.py (idempotency key, dedup header) mitigates it either. Severity 5 (money duplication) and effort M (needs idempotency key design, not a one-liner) both look right.",
    "severity": 5,
    "effort": "M",
    "trap_matched": null,
    "checked": [
      "whether _seen or another mechanism addresses gateway-side retries",
      "gateway.py for idempotency key support",
      "ticket reference in code/comments"
    ],
    "opened": [
      "src/pay/gateway.py",
      "src/pay/ledger.py",
      "tests/test_refund.py"
    ]
  },
  {
    "fingerprint": "c716d4382547d1ce",
    "verdict": "confirm",
    "proof": "Repo-wide grep for `issue_partial` (case-sensitive, all of src/ and tests/) returns only the definition at refund.py:45 -- no callers, no test coverage, no dynamic dispatch/reflection/string-based lookup found. tests/test_refund.py exercises validate, audit_trail, and has a skipped issue test, but nothing calls issue_partial. It is a plain function, not a plugin hook or serialized entry point.",
    "severity": 3,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "reflection/string-dispatch",
      "test runner discovery (not a test-named function)",
      "public/plugin surface (none, internal module)"
    ],
    "opened": [
      "tests/test_refund.py",
      "tests/conftest.py"
    ]
  },
  {
    "fingerprint": "441a5776ddef0261",
    "verdict": "confirm",
    "proof": "Repo-wide grep for `fingerprint` returns only the definition at utils.py:11 -- zero callers anywhere in src/ or tests/. Not referenced by name in serialization, routing, or DI. It also uses md5, a weak hash, compounding the dead-code concern, but the core claim (unused) is verified as stated.",
    "severity": 2,
    "effort": "S",
    "trap_matched": null,
    "checked": [
      "reflection/string-dispatch",
      "public/plugin surface",
      "test discovery"
    ],
    "opened": []
  }
]
